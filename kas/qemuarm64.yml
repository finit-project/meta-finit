header:
  version: 14

distro: poky

repos:
    url: git://git.yoctoproject.org/poky.git
    commit: scarthgap
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:

  meta-finit:
    url: https://github.com/liuming50/meta-finit.git
    commit: scarthgap

local_conf_header:
  base: |
    CONF_VERSION = "2"
    DISTRO_FEATURES:append = " finit"
    INIT_MANAGER = "finit"

machine: qemuarm64

target:
  - core-image-minimal
